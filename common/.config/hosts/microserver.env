# microserver -- headless, reached over SSH. No graphical session.
#
# The tty1 guard in .bash_profile already means an SSH login (/dev/pts/N)
# never triggers an autostart; leaving this empty states the intent anyway.
export AUTOSTART_SESSION=
