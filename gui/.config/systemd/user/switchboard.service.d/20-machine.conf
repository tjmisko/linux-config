# Host-local binary path (docs/machine-configuration.md). This machine builds
# from a private development checkout into ~/.config/switchboard/bin instead
# of `go install`ing to ~/go/bin.
[Service]
Environment=SWITCHBOARD_BIN=%h/.config/switchboard/bin/switchboard
