/* Global rofi style. The real thing lives in terminal.rasi so per-picker themes
   can import the same base; keep this file to the import plus any
   configuration{} defaults. */

@import "/home/tjmisko/.config/rofi/terminal.rasi"
