# Candidate window appearance. Force the dark theme unconditionally (do not
# follow the system light/dark setting).
Theme=default-dark
DarkTheme=default-dark
UseDarkTheme=False
# Candidate/preedit popup font (drives both text and window size).
Font="Sans 13"
# Vertical list so Up/Down move between candidates as expected.
Vertical Candidate List=True
