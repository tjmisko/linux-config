# Unicode/emoji picker. Trigger opens a search box over the active input
# method; type a name (e.g. "fire", "heart", "alpha") to insert emoji/symbols.
# Bound to Ctrl+Alt+E to match the old ibus emoji hotkey.
[TriggerKey]
0=Control+Alt+E
